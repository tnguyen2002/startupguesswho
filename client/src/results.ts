/**
 * Images shown on the end-of-game screen. Files live in client/public/results/.
 * Add more by dropping a file in that folder and listing it here; the screen
 * crossfades through every entry in order.
 */
export const LOSER_IMAGES = ['/results/loser-1.png'];
export const WINNER_IMAGES = ['/results/winner-1.png'];
